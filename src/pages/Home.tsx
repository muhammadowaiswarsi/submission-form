import { SubmissionForm } from "../components/submission";

function Home() {
  return (
    <main className="app-shell">
      <div className="dashboard">
        <SubmissionForm />
      </div>
    </main>
  );
}

export default Home;
