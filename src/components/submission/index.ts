/**
 * Submission dashboard feature — import from here for stable public API.
 */
export { default as SubmissionForm } from "./SubmissionForm";
export type { FilterType, SubmissionRecord, SubmissionType } from "./types";
